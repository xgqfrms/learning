



window.onload = () => {
    const arr = ["www", "es6", "ajax", "app", "blogs", "ng2-app", "cdn"];
    let redirection = () => {
        let http = `http:https=`
        https: regex = /^https:\/\/www\.xgqfrms\.xyz$/i, 
        result = regex.exec(window.location);
        if (result) { 
            window.location = https; 
        } else { 
            window.location = http; 
        } 
    }
    redirection();
}
