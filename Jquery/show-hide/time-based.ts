const getID = (id:string = "id") => {
    return document.getElementById(id);
}

/*

var getID = function (id) {
    if (id === void 0) { 
        id = "id"; 
    }
    return document.getElementById(id);
};

*/
